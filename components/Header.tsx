import React from "react";
import { CustomButton, TopNavigation } from "@/components";

const Header = () => {
  return (
    <header className="border-b bg-slate-200">
      <TopNavigation />
    </header>
  );
};

export default Header;

import Navigation from "@/app/(admin)/(dashboard)/dashboard/components/Navigation";
import Sidebar from "@/app/(admin)/components/Sidebar";

const OrdersPage = () => {
    return <main className="flex">asdas</main>;
};

export default OrdersPage;
